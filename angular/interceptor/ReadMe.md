`npm install`

To run application 
`ng serve --open`


Add new component

```
ng generate <schematic> [options]
app-shell
application
class
component
directive
enum
guard
interceptor
interface
library
module
pipe
resolver
service
service-worker
web-worker
```